/**
 * Card
 *
 * Demonstrates a simple UI component that can be imported
 * through a barrel file.
 *
 * This component intentionally does not introduce any
 * duplicate DOM attributes.
 */
export default function Card() {
  return (
    <section id="dashboard-card">
      Dashboard
    </section>
  );
}
